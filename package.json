{
  "name": "praxihub-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "functions"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
